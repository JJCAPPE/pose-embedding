# Test fixtures

Fixtures in this repository must be tiny, synthetic, and safe to redistribute.
The test suite generates most arrays at runtime so no NTU samples, MotionBERT
weights, or derived licensed features are stored here.
